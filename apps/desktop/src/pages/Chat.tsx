/**
 * Chat page - AI conversation interface
 */

import { ChatCard } from '@/components/chat'

export function Chat() {
  return (
    <main className="content-card h-full overflow-hidden">
      <ChatCard />
    </main>
  )
}
